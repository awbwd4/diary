22.05.02
   1. 파이썬, 알파벳 문자의 순서를 찾는 방법
     - int(ord(input_data[0])) - int(ord(‘a’)) + 1
     - 이렇게 하면 input_data의 값이 d인 경우 숫자 4가 나온다.
   2. ’수’ 와 ‘숫자’구분 명확히
     - 수 : 12,100,774 등
     - 숫자 : 0~9 까지의 10개의 문자들
   3.  내장함수들
     - sort() : 문자열들인 경우에도 순서대로 소팅해준다.
     - .isalpha() : 해당문자열이 알파벳인지 아닌지 
     - .isnumeric() : 해당 문자열이 숫자인지 아닌지.
     - .join() : . 문자열들을 연결해줌.
